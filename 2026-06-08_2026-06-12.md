# SQL LeetCode 練習筆記

**練習日期：** 2026-06-08 / 2026-06-12
**題目數量：** 2 題（262, 550）
**主題：** Boolean 運算式、DATE_ADD、相關子查詢、分數計算

---

## 題目總覽

| # | 題目 | 難度 | 核心概念 |
|---|------|------|----------|
| 262 | Trips and Users | Hard | Boolean 在 SUM 內、多次 JOIN 同張表 |
| 550 | Game Play Analysis IV | Medium | DATE_ADD、相關子查詢、分數計算 |

---

## 262. Trips and Users

🔗 https://leetcode.com/problems/trips-and-users/

### 解法

```sql
SELECT 
    t.request_at AS Day,
    ROUND(SUM(t.status != 'completed') / COUNT(*), 2) AS "Cancellation Rate"
FROM Trips t
JOIN Users client ON (t.client_id = client.users_id AND client.banned != 'YES')
JOIN Users driver ON (t.driver_id = driver.users_id AND driver.banned != 'YES')
WHERE t.request_at BETWEEN '2013-10-01' AND '2013-10-03'
GROUP BY t.request_at;
```

### 重點

- `SUM(t.status != 'completed')`：在 MySQL 裡，Boolean 運算式的結果是 0 或 1，所以可以直接放進 `SUM()` 來計算「符合條件的列數」。這裡就是計算「沒有完成的行程數」。
- 取消率 = 取消數 / 總數，所以是 `SUM(...) / COUNT(*)`，再用 `ROUND` 保留兩位小數。
- 同一張 Users 表 JOIN 兩次，分別當作乘客和司機，用不同 alias（`client`、`driver`）區分。
- JOIN 條件裡直接加上 `banned != 'YES'`，這樣被封鎖的用戶從一開始就不會進來，比在 `WHERE` 裡過濾更乾淨。

> `SUM(條件)` 這個技巧很實用，等同於「計算符合條件的列數」，可以取代 `COUNT(CASE WHEN ...)`。

---

## 550. Game Play Analysis IV

🔗 https://leetcode.com/problems/game-play-analysis-iv/

### 解法

```sql
SELECT ROUND(
    COUNT(DISTINCT a.player_id) / (SELECT COUNT(DISTINCT player_id) FROM Activity), 2
) AS fraction
FROM Activity a
JOIN Activity next_day
    ON a.player_id = next_day.player_id
    AND next_day.event_date = DATE_ADD(a.event_date, INTERVAL 1 DAY)
WHERE a.event_date = (
    SELECT MIN(event_date)
    FROM Activity
    WHERE player_id = a.player_id
);
```

### 錯誤紀錄

❌ 第一次的錯誤寫法：

```sql
AND a.event_date = next_day.event_date - 1
```

✅ 正確寫法：

```sql
AND next_day.event_date = DATE_ADD(a.event_date, INTERVAL 1 DAY)
```

日期不能直接用 `- 1` 做運算，要用 `DATE_ADD(日期, INTERVAL 1 DAY)` 才是正確的 MySQL 日期加法。

### 重點

- 題目要找「第一天登入後，隔天也有登入」的玩家比例。
- 步驟拆解：
  1. `WHERE a.event_date = (SELECT MIN(...))` — 篩選出每個玩家的第一天登入記錄
  2. `JOIN Activity next_day ON ... DATE_ADD(...)` — 找這個玩家隔天是否也有登入記錄
  3. `COUNT(DISTINCT a.player_id)` — 計算符合條件的玩家數
  4. 除以全部玩家數，得到比例
- `DATE_ADD` 和之前用過的 `INTERVAL` 是同一個概念，只是寫法不同：
  - `DATE_ADD(date, INTERVAL 1 DAY)` — 函數寫法
  - `date + INTERVAL 1 DAY` — 運算子寫法（197 題用過）

---

## 今日學習重點總結

| 概念 | 說明 | 範例題 |
|------|------|--------|
| `SUM(條件)` | Boolean 結果為 0/1，可直接加總來計算符合條件的列數 | 262 |
| JOIN 條件內過濾 | 把過濾邏輯寫在 `ON` 裡，比事後在 `WHERE` 過濾更早排除不需要的資料 | 262 |
| `DATE_ADD` | 日期加法，不能直接用 `+ 1` 或 `- 1` | 550 |

### SUM(條件) vs COUNT(CASE WHEN)

```sql
-- 兩種寫法結果相同，SUM(條件) 更簡潔
SUM(status != 'completed')
COUNT(CASE WHEN status != 'completed' THEN 1 END)
```

### 日期加減的正確寫法

```sql
DATE_ADD(date, INTERVAL 1 DAY)   -- 加一天（函數寫法）
date + INTERVAL 1 DAY             -- 加一天（運算子寫法）
DATE_ADD(date, INTERVAL -1 DAY)  -- 減一天
DATEDIFF(date1, date2)           -- 兩個日期相差幾天
```
