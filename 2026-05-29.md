# SQL LeetCode 練習筆記

**練習日期：** 2026-05-29
**題目數量：** 3 題（176, 177, 178）
**主題：** 子查詢、Window Function、DENSE_RANK、CREATE FUNCTION

---

## 題目總覽

| # | 題目 | 難度 | 核心概念 |
|---|------|------|----------|
| 176 | Second Highest Salary | Medium | 子查詢 + MAX |
| 177 | Nth Highest Salary | Medium | CREATE FUNCTION + OFFSET |
| 178 | Rank Scores | Medium | Window Function、DENSE_RANK |

---

## 176. Second Highest Salary

🔗 https://leetcode.com/problems/second-highest-salary/

### 解法

```sql
SELECT MAX(salary) AS SecondHighestSalary
FROM Employee
WHERE salary < (SELECT MAX(salary) FROM Employee);
```

### 重點

- 先用子查詢找出最高薪水，再從「小於最高薪水」的資料裡取 `MAX()`，就是第二高。
- 這個方法的好處是：如果沒有第二高（只有一個人或所有薪水相同），`MAX()` 會回傳 NULL，符合題目要求。

---

## 177. Nth Highest Salary

🔗 https://leetcode.com/problems/nth-highest-salary/

### 解法

```sql
CREATE FUNCTION getNthHighestSalary(N INT) RETURNS INT
BEGIN
    DECLARE offsetN INT;
    SET offsetN = N - 1;
    RETURN (
        SELECT DISTINCT salary
        FROM Employee
        ORDER BY salary DESC
        LIMIT 1 OFFSET offsetN
    );
END
```

### 重點

- 176 題的延伸，改成找第 N 高。
- `LIMIT 1 OFFSET N-1`：跳過前 N-1 筆，取第 N 筆。
  - 例如找第 2 高：`LIMIT 1 OFFSET 1`（跳過第 1 筆，取第 2 筆）
- 為什麼要 `DECLARE offsetN` 另外存？因為 MySQL 的 `OFFSET` 不能直接寫運算式（`OFFSET N-1` 會報錯），要先算好存成變數再用。
- `SELECT DISTINCT` 是為了去除重複薪水，確保第 N 高是真正的第 N 個不同的值。

---

## 178. Rank Scores

🔗 https://leetcode.com/problems/rank-scores/

### 解法

```sql
SELECT score, DENSE_RANK() OVER (ORDER BY score DESC) AS "rank"
FROM Scores;
```

### 重點

- 這題第一次用到 **Window Function（視窗函數）**，是一個很重要的概念。
- `DENSE_RANK() OVER (ORDER BY score DESC)`：依照分數由高到低排名。
- **DENSE_RANK vs RANK 的差別**：
  - `RANK()`：並列第 1 之後，下一名直接跳到第 3（1, 1, 3, 4）
  - `DENSE_RANK()`：並列第 1 之後，下一名是第 2，名次連續不跳號（1, 1, 2, 3）
- 題目要求名次連續，所以用 `DENSE_RANK`。

> Window Function 的基本語法：`函數() OVER (ORDER BY 欄位)`，之後會常用到。

---

## 今日學習重點總結

| 概念 | 說明 | 範例題 |
|------|------|--------|
| 子查詢取第 N 高 | `WHERE salary < MAX` 找第二高；`LIMIT 1 OFFSET N-1` 找第 N 高 | 176, 177 |
| `DECLARE` / `SET` | MySQL Function 內宣告變數並賦值 | 177 |
| Window Function | `函數() OVER (ORDER BY ...)` 不分組直接對每列計算排名 | 178 |
| `DENSE_RANK` vs `RANK` | DENSE_RANK 名次連續不跳號；RANK 有並列時會跳號 | 178 |

### LIMIT / OFFSET 語法

```sql
LIMIT 1 OFFSET 0   -- 第 1 高（跳過 0 筆）
LIMIT 1 OFFSET 1   -- 第 2 高（跳過 1 筆）
LIMIT 1 OFFSET N-1 -- 第 N 高（跳過 N-1 筆）
```

### DENSE_RANK vs RANK 比較

```
分數：100, 100, 90, 80

RANK()       → 1, 1, 3, 4   （跳號）
DENSE_RANK() → 1, 1, 2, 3   （不跳號）
```
