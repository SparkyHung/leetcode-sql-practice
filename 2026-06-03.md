# SQL LeetCode 練習筆記

**練習日期：** 2026-06-03
**題目數量：** 3 題（180, 184, 185）
**主題：** Self JOIN 連續值、相關子查詢、Window Function PARTITION BY

---

## 題目總覽

| # | 題目 | 難度 | 核心概念 |
|---|------|------|----------|
| 180 | Consecutive Numbers | Medium | Self JOIN 三次 |
| 184 | Department Highest Salary | Medium | 相關子查詢 |
| 185 | Department Top Three Salaries | Hard | 相關子查詢 / DENSE_RANK + PARTITION BY |

---

## 180. Consecutive Numbers

🔗 https://leetcode.com/problems/consecutive-numbers/

### 解法

```sql
SELECT DISTINCT l1.num AS ConsecutiveNums
FROM Logs AS l1
JOIN Logs AS l2 ON l1.id = l2.id - 1
JOIN Logs AS l3 ON l1.id = l3.id - 2
WHERE l1.num = l2.num AND l1.num = l3.num;
```

### 重點

- 把同一張表 JOIN 三次，分別代表「第一筆」、「第二筆」、「第三筆」。
- `l1.id = l2.id - 1`：l2 是 l1 的下一列；`l1.id = l3.id - 2`：l3 是 l1 的下下列。
- `WHERE` 確認三列的 num 都相同，就是連續出現三次。
- `DISTINCT` 是為了避免同一個數字因為多組連續而重複出現。

> Self JOIN 不只能比較父子關係，也可以用來比較「相鄰的列」。

---

## 184. Department Highest Salary

🔗 https://leetcode.com/problems/department-highest-salary/

### 解法

```sql
SELECT d.name AS Department, e.name AS Employee, e.salary AS Salary
FROM Employee AS e
LEFT JOIN Department d ON e.departmentId = d.id
WHERE e.salary = (
    SELECT MAX(salary)
    FROM Employee
    WHERE departmentId = e.departmentId
);
```

### 重點

- 子查詢裡用了 `e.departmentId`，這是來自外層查詢的值，這種寫法叫做**相關子查詢（Correlated Subquery）**。
- 每一列都會執行一次子查詢，去找「同部門的最高薪水」，再比對這列的薪水是否等於最高。
- 相關子查詢的特點：子查詢依賴外層的當前列，沒辦法獨立執行。

---

## 185. Department Top Three Salaries

🔗 https://leetcode.com/problems/department-top-three-salaries/

### 解法一：相關子查詢

```sql
SELECT d.name AS Department,
       e.name AS Employee,
       e.salary AS Salary
FROM Employee e
JOIN Department d ON e.departmentId = d.id
WHERE (
    SELECT COUNT(DISTINCT salary)
    FROM Employee
    WHERE departmentId = e.departmentId AND salary > e.salary
) < 3;
```

**邏輯：** 計算同部門中「比這個薪水更高的不同薪水有幾個」，如果少於 3 個，表示這個薪水是前三高。

### 解法二：DENSE_RANK + PARTITION BY（推薦）

```sql
SELECT Department, Employee, Salary
FROM (
    SELECT
        d.name AS Department,
        e.name AS Employee,
        e.salary AS Salary,
        DENSE_RANK() OVER (
            PARTITION BY e.departmentId
            ORDER BY e.salary DESC
        ) AS rnk
    FROM Employee e
    JOIN Department d ON d.id = e.departmentId
) AS Ranked
WHERE rnk <= 3;
```

**邏輯：** 用 `DENSE_RANK()` 在每個部門內分別排名，再從外層篩選排名前三的。

### 重點

- **`PARTITION BY` 的用途**：把 Window Function 的計算範圍限制在每個分組內。
  - 上一題（178）的 `DENSE_RANK()` 是對全表排名
  - 這題加了 `PARTITION BY departmentId`，變成「每個部門內分別排名」
- 兩種解法都對，但解法二更直觀，效能通常也比相關子查詢好。
- 解法二的架構是「先在子查詢裡加上排名欄位，再從外層用 `WHERE` 篩選」，這個模式之後會很常用。

> `PARTITION BY` 就像 `GROUP BY`，但用在 Window Function 裡，不會把多列合併成一列。

---

## 今日學習重點總結

| 概念 | 說明 | 範例題 |
|------|------|--------|
| Self JOIN 比較相鄰列 | JOIN 同張表多次，用 id 偏移來對齊相鄰列 | 180 |
| 相關子查詢 | 子查詢引用外層當前列的值，每列都執行一次 | 184, 185 |
| `PARTITION BY` | Window Function 內的分組，各組獨立計算排名 | 185 |

### PARTITION BY 語法

```sql
DENSE_RANK() OVER (
    PARTITION BY 分組欄位    -- 按這個欄位分組，各組獨立排名
    ORDER BY 排序欄位 DESC   -- 組內排序方式
)
```

### 解法二的常用模式

```sql
-- 先在子查詢加排名，再從外層篩選
SELECT ...
FROM (
    SELECT ..., DENSE_RANK() OVER (PARTITION BY ... ORDER BY ...) AS rnk
    FROM ...
) AS Ranked
WHERE rnk <= N;
```
